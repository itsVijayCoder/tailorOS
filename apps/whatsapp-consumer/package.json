{
  "name": "@tailoros/whatsapp-consumer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit --pretty false",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@tailoros/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "wrangler": "^4.54.0"
  }
}
